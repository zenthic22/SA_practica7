apiVersion: v1
kind: Service
metadata:
  name: gateway
  namespace: sa-p5
  labels: { app: gateway }
spec:
  selector:
    app: gateway
  type: NodePort
  ports:
    - name: http
      port: 4001
      targetPort: http
      nodePort: 30081   # <- expuesto en los nodos